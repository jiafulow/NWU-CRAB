#!/bin/bash

DIR=MC2
multicrab -submit 500 -c ${DIR}
multicrab -submit 500 -c ${DIR}
multicrab -submit 500 -c ${DIR}

#DIR=MC1/ZZ/
#crab -submit 500 -c ${DIR}
#crab -submit 500 -c ${DIR}
#crab -submit 500 -c ${DIR}
#crab -submit 500 -c ${DIR}
#crab -submit 500 -c ${DIR}
